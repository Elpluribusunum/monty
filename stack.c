#include "monty.h"
/**
 * f_stack - images on the top
 * @head: pile head
 * @counter: line_number
 * Return: the end
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
